function get_documents(document_path)
    
end

function chunker(documents, chunk_size, overlap)
    
end

function db_embedder(chunks, db_embedding_model)
    
end

function set_arrow_store(chunks)
end

function set_vector_db(document_embeddings)

end

